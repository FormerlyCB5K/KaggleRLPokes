# Historical or Deferred Specifications

These documents are not current implementation contracts.

- [`08-checkpoint-resume.md`](08-checkpoint-resume.md) targets a separate
  `train_rules-2.py` script that is not present. The active cluster trainer already has
  checkpoint/resume support, so this proposal is retained only as possible future
  reference.
