# Prototypes

This folder contains experimental implementations that are not wired into the active
runtime. `pokemon_encoder.py` is the superseded standalone generalized Pokémon encoder;
the production representation is implemented in [`../../features.py`](../../features.py).
